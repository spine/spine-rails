class App.<%= class_name %> extends Spine.Controller
  #elements:
  #  ".items": items

  constructor: ->
    super
    # Do the initialization here

  refresh: ->
    # Do the rendering here
    # You can use shortcut to render the views:
    # @generate 'show', model
